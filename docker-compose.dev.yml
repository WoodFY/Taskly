version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: taskly-mongo-dev
    restart: unless-stopped
    ports:
      - '27017:27017'
    volumes:
      - mongodb_dev_data:/data/db

volumes:
  mongodb_dev_data:
