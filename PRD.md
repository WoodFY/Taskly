# 项目需求文档 (PRD)

## 1. 项目概况
- **技术栈**:
    - 前端: Vue 3 + TypeScript + Less + Pinia + vue-i18n
    - 后端: NestJS + MongoDB (Mongoose) + JWT
    - 运维: Docker
- **目标**: 构建一个支持中英双语、具有优雅 UI/UX 的个人任务管理系统。

## 2. 核心功能需求
### 2.1 用户认证
- **注册/登录**: 用户通过邮箱/密码注册和登录。
- **鉴权**: 后端使用 JWT 生成 Token，前端通过 Pinia + localStorage 持久化存储 Token。
- **安全**: 所有非公开接口必须集成认证拦截器 (Guard)，验证请求头中的 Token。

### 2.2 任务管理 (CRUD)
- **创建**: 用户可以创建新任务（标题、描述、状态、截止日期）。
- **读取**: 获取当前用户的任务列表，支持按状态（进行中/已完成）筛选。
- **更新**: 修改任务详情或标记任务为完成。
- **删除**: 软删除或硬删除任务。

### 2.3 国际化与样式
- **多语言**: 使用 `vue-i18n` 实现一键切换中/英文界面。
- **主题**: 使用 Less 变量管理主题色，支持嵌套样式，确保 UI 优雅统一。

## 3. 非功能需求
- **文档**: 接口需符合 Apifox 定义的文档标准（需手动同步或参考）。
- **部署**: 项目需包含 `Dockerfile`，支持 `docker build` 和 `docker run` 进行容器化调试。
- **规范**: 严格遵守 `CLAUDE.md` 中的命名和代码风格规范。